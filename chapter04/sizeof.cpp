#include <iostream>

using namespace std;

int main()
{
	// int a[10] = {0};
	// int count = sizeof(a)/sizeof(*a);
	// cout << count << endl;

	const char *cp;
	// char *q = static_cast<char*>(cp);
	// char *q = const_cast<char*>(cp);
	
	return 0;
}